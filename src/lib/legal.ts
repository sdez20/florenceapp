// Living-document versions. Bump a version when the Terms or Privacy Policy
// MATERIALLY change — the consent gate compares these to what the user agreed
// to and re-prompts if they differ. Client-safe (no filesystem access here).
export const TERMS_VERSION = "1.0";
export const PRIVACY_VERSION = "1.0";

// Shown on the policy pages. Replace before launch (flagged placeholder).
export const LEGAL_LAST_UPDATED = "[DATE]";
