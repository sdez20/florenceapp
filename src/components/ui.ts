// Shared Tailwind class strings, so every screen draws from one source of truth.

/** The deep-olive uppercase call-to-action button used across the app. */
export const cta =
  "block w-full rounded-[18px] border-[1.5px] border-olive bg-ink px-5 py-[18px] text-center font-sans text-[13px] font-semibold uppercase tracking-[0.18em] text-paper transition-[transform,background-color] duration-300 hover:-translate-y-px hover:bg-olive focus-visible:outline-2 focus-visible:outline-offset-[3px] focus-visible:outline-clay";

/** The small clay eyebrow label that sits above headings. */
export const eyebrow =
  "text-[11px] font-semibold uppercase tracking-[0.28em] text-clay";
